# Netlify deployment

This project is configured for GitHub-connected Netlify deployment.

- Base directory: leave blank
- Build command: npm run build
- Publish directory: .next
- Node version: 20

The package lock uses the public npm registry. Do not add NPM_CONFIG_PRODUCTION.
